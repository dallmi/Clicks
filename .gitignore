# Input files (KQL exports, too large for git)
input/*.xlsx
input/*.xls
input/*.csv

# Data files (DuckDB)
data/*.db
data/*.db.wal

# Output files
output/*.parquet

# Standalone dashboard build (embeds the events data — keep local only)
output/*standalone*.html

# Python
__pycache__/
*.pyc

# macOS
.DS_Store

# Local image assets (not for git)
Bilder/
